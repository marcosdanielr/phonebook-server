{
  "extends": "@adonisjs/tsconfig/tsconfig.app.json",
  "compilerOptions": {
    "rootDir": "./",
    "outDir": "./build",
    "baseUrl": "./app",
    "paths": {
      "#use_cases/*": ["use_cases/*.ts"],
      "#lib/*": ["lib/*.ts"],
      "#repositories/*": ["repositories/*.ts"]
    }
  }
}
